import { OrganizationSchema } from '#database/schema'

/**
 * Lucid model for the `organizations` table.
 * In this product, an Organization is the Tenant.
 */
export default class Organization extends OrganizationSchema {}
